const path = require("path");

module.exports = {
  "seeders-path": path.resolve("src/lib/database/sequelize", "seeders"),
  "migrations-path": path.resolve("src/lib/database/sequelize", "migrations"),
  config: path.resolve("src/lib/database", "config.json"),
};
